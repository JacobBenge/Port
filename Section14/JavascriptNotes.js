// Javascript notes

//OPERATORS

	// clear the console
	clear()
	// returns the data type of the input. ex. typeof 4 = "number". ex. typeof 0/0 = NaN
	typeof

// STORING VARIABLES

	//basic syntax for variable. The var keyword is the old way. Cannot start variable name with a number. Use camelCase.
	let year = 1985;
	var year = 1985;
	
	// boolean logic. Using "is" in boolean name helps show its a variable. Ex.  isGameOver 
	true
	false
	let isActive = true;
	
	// basic syntax for constant. Great for conversions.
	const pi = 3.14159;
	
	//incremetation and decrementation
	score += 10;
	score *= 10;
	score -= 10;
	score /= 10;
	score--;
	score++;